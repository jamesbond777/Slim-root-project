<nav class="navbar navbar-default" style="background-color:#719e40;color:white;z-index: 1;">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"
                    aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path_for('home') }}"  style="color:white;font-family: inherit;"><b>Slim 3</b></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li class="active"><a style="color:white;font-family: inherit;" class="bold" href="#">Documentation<span class="sr-only">(current)</span></a></li>
                <li><a href="#" style="color:white;font-family: inherit;" class="bold">Support</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <form class="navbar-form navbar-left" role="search">
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="Search">
                    </div>
                    <button style="background-color:#719e40;color:white" type="submit" class="btn btn-default">Submit</button>
                </form>
                {% if username == 'visitor' %}
                <li><a href="{{ path_for('login') }}" style="color:white;font-family: inherit;" class="bold" href="#">Log in</a></li>
                {% else %}
                <li><a href="{{ path_for('logout') }}" style="color:white;font-family: inherit;" class="bold" href="#">{{ username }} / Log out</a></li>
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
